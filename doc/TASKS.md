# Task Tracker

- [x] Build payment initiation and approval UI in `/frontend`, including live account/counterparty/payment queries, slide-over workflows, and approval actions.
- [x] Implement a Supabase-backed background job queue, scheduler, treasury workers, and admin job visibility endpoints in `/backend`.
- [x] Build Claude-powered cash flow forecasting across `/backend` and `/frontend`, including async generation, stress scenarios, accuracy tracking, and forecast visualization.
- [x] Implement live cash-position aggregation, summary/history APIs, and dashboard/account wiring across `/backend` and `/frontend`.
- [x] Build bank statement import, parser coverage, transaction ingestion, reconciliation services, and the import wizard across `/backend` and `/frontend`.
- [x] Build the Treasury Copilot chat workspace with Claude tool-use orchestration, encrypted session persistence, SSE streaming APIs, and the dashboard copilot interface across `/backend`, `/frontend`, and `/supabase`.
- [x] Build the treasury reporting suite with SQL-backed cash summary, liquidity, and compliance reporting across `/backend`, `/frontend`, and `/supabase`.
- [x] Build the admin audit log viewer with paginated filtering, JSON diff expansion, and CSV export across `/backend` and `/frontend`.
- [x] Build the admin console for users, roles, and treasury policy governance across `/backend` and `/frontend`.
- [x] Build the liquidity management feature with pool management, sweep execution, liquidity position analysis, intercompany loan workflows, and dashboard coverage across `/backend`, `/frontend`, and `/supabase`.
- [x] Build the risk management engine with persisted exposure calculations, breach alerting, async recalculation, and a live risk dashboard across `/backend`, `/frontend`, and `/supabase`.
- [x] Build the in-app notification system with Supabase-backed persistence, realtime delivery, delivery/retention workers, and dashboard notification surfaces across `/backend`, `/frontend`, and `/supabase`.
- [x] Build the treasury policy enforcement engine with active payment blocking, admin CRUD/validation APIs, approval escalation wiring, and visual policy editing across `/backend`, `/frontend`, and `/supabase`.
